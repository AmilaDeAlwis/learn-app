namespace backend.Enum
{
    // Enum for Nationality
    public enum Nationality
    {
        Select, // Default value
        American,
        British,
        Canadian,
        Chinese,
        French,
        German,
        Indian,
        Japanese,
        Russian,
    }

    // Enum for Residency Status
    public enum Recidency
    {
        Select, // Default value
        Citizen,
        PermanentResident,
        TemporaryResident,
        StudentVisa,
    }

    // Enum for Gender
    public enum Gender
    {
        Select, // Default value
        Male,
        Female,
        NonBinary,
        Custom,
    }
}
